class Main {
    function void main() {
        var int result;
        let result = Main.fib(4);
        while (true) {
        }
        return;
    }
    function int fib(int n) {
        if (n < 2) { return n; }
        return Main.fib(n - 1) + Main.fib(n - 2);
    }
}